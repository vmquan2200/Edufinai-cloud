services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: finance-edu-frontend
    ports:
      - "3000:80"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
    networks:
      - finance-edu-network

networks:
  finance-edu-network:
    driver: bridge

