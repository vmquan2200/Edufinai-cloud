# ============================================
# Railway Environment Variables cho Auth Service
# Copy các biến này vào Railway Dashboard → Variables
# ============================================

# Database Connection (Reference từ MySQL Service)
# Thay "MySQL" bằng tên service MySQL của bạn trên Railway
MYSQLHOST=${{MySQL.RAILWAY_PRIVATE_DOMAIN}}
MYSQLPORT=3306
MYSQLDATABASE=identity
MYSQLUSER=root
MYSQLPASSWORD=${{MySQL.MYSQL_ROOT_PASSWORD}}

# Eureka Server Connection (Reference từ Eureka Service)
# Thay "Eureka" bằng tên service Eureka của bạn trên Railway
# Hoặc thay bằng URL public domain thực tế: https://eureka-production.up.railway.app/eureka/
EUREKA_CLIENT_SERVICE_URL_DEFAULTZONE=${{Eureka.RAILWAY_PUBLIC_DOMAIN}}/eureka/

# Service Instance Configuration
EUREKA_INSTANCE_HOSTNAME=auth-service
EUREKA_INSTANCE_IP=auth-service
EUREKA_SECURE_PORT_ENABLED=true
EUREKA_NON_SECURE_PORT_ENABLED=false

# Spring Profile
SPRING_PROFILES_ACTIVE=railway

# JWT Configuration (Optional - đã có default trong code)
JWT_SECRET=1TjXchw5FloESb63Kc+DFhTARvpWL4jUGCwfGWxuG5SIf/1y/LgJxHnMqaF6A/ij
JWT_VALID_DURATION=3600
JWT_REFRESHABLE_DURATION=36000

# ============================================
# LƯU Ý:
# 1. Railway tự động inject PORT và RAILWAY_* variables
# 2. Đảm bảo MySQL và Eureka services đã được deploy trước
# 3. Nếu service names khác, thay "MySQL" và "Eureka" bằng tên thực tế
# 4. Format reference: ${{ServiceName.VARIABLE_NAME}}
# ============================================
